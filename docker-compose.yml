services:
  web:
    container_name: apphecorpcom
    build:
      context: .
      dockerfile: Dockerfile
    image: apphecorpcom
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
